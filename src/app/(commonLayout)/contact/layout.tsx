export default function BranchLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            <div>Hold your breath and count to 10...</div>
            {children}
        </>
    );
}